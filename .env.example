# ============================================
# Supabase Configuration
# ============================================
# Get your values from: https://app.supabase.com/project/_/settings/api

# Your Supabase project URL
VITE_SUPABASE_URL=https://your-project.supabase.co

# Your Supabase anon/public key (safe to expose in frontend)
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# ============================================
# SECURITY WARNINGS
# ============================================
# 1. NEVER commit the actual .env file to git
# 2. NEVER use service-role keys in frontend code
# 3. Service-role keys should ONLY be in backend/Edge Functions
# 4. For production, set these in your deployment platform (Vercel, Netlify, etc.)
# 5. The anon key is safe to expose - it's protected by Row Level Security (RLS)

# ============================================
# Local Development
# ============================================
# Copy this file to .env and fill in your actual values
# The app will fail to start if these variables are missing (by design for security)
